# Agent skills bridge

Use this folder for toolchains that resolve skills from `.agents/skills/`.

Canonical skill content lives in:

- `.github/skills/` (Copilot-style skill layout)
- `.claude/skills/` (Claude runtime skill layout)

When adding a new skill family, ensure all three surfaces stay aligned.