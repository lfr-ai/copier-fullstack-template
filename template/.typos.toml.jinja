[default]
locale = "en-us"

[default.extend-words]
{{ project_slug }} = "{{ project_slug }}"

cfg = "cfg"
ctx = "ctx"
env = "env"
impl = "impl"
init = "init"
msg = "msg"
fmt = "fmt"
src = "src"

nd = "nd"

hel = "hel"

[files]
extend-exclude = [
    "bun.lock",
    "*.lock",
    "*.egg-info/**",
    "*.baseline",
    "backend/alembic/versions/**",
    "logs/**",
    "htmlcov/**",
    "jscpd-report/**",
    "CHANGELOG.md",
]
