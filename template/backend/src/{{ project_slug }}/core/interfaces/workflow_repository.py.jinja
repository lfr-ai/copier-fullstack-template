"""Workflow repository -- domain persistence contract for workflows."""

from __future__ import annotations

from typing import Protocol, runtime_checkable

from {{ project_slug }}.core.entities.workflow import Workflow
from {{ project_slug }}.core.interfaces.repository import Repository


@runtime_checkable
class WorkflowRepository(Repository[Workflow], Protocol):
    """Repository defining workflow persistence operations.

    Extends the generic :class:`Repository` with workflow-specific
    lookup methods.
    """

    async def get_by_name(self, *, name: str) -> Workflow | None:
        """Retrieve workflow by unique name."""
        ...
