"""Document loader gateway — abstract interface for document ingestion.

Concrete adapters may use LlamaIndex, Azure Document Intelligence,
LangChain loaders, etc.
"""

from __future__ import annotations

from typing import Protocol, runtime_checkable

from {{ project_slug }}.core.entities.document import Document
@runtime_checkable
class DocumentLoaderGateway(Protocol):
    """Gateway for loading and parsing documents."""

    async def load(self, source: str) -> list[Document]:
        """Load and parse a document source into Document entities.

        'source' may be a file path, URL, or other resource identifier.
        """
        ...

    async def load_many(self, sources: list[str]) -> list[Document]:
        """Load multiple document sources."""
        ...
