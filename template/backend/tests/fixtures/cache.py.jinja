"""Cache test fixtures."""

from __future__ import annotations

import pytest

from {{ project_slug }}.infrastructure.cache.memory_cache import MemoryCacheAdapter


@pytest.fixture
def cache() -> MemoryCacheAdapter:
    """Create fresh in-memory cache for testing.

    Returns:
        MemoryCacheAdapter: Empty memory cache adapter.
    """
    return MemoryCacheAdapter()
