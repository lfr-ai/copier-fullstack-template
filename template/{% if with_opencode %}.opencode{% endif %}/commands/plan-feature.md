---
description: Plan a feature using the coordinator workflow
agent: coordinator
---

Analyze the following feature request and create an implementation plan.

Use the @planner subagent to break it into tasks, then use the @architect subagent to
validate the plan against clean / hexagonal architecture patterns.

Feature: $ARGUMENTS

Steps:

1. Gather codebase context for affected areas
2. Create a structured plan with ordered tasks
3. Validate against architecture rules
4. Present the plan for approval before any implementation
