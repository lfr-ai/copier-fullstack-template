{%- if with_opencode %}
# Project Architecture Context

## Structure
```
backend/src/{{ project_slug }}/
├── core/           # Pure domain (entities, enums, interfaces, value objects)
├── application/    # Use cases, services, DTOs
├── presentation/   # API routes, CLI, web controllers
├── infrastructure/ # DB repos, cache, HTTP clients, auth, profiling
├── composition/    # Dependency injection wiring
├── config/         # Settings, logging, constants
{%- if use_ai %}
├── ai/             # AI vertical: LLM, agents, RAG, MCP
{%- endif %}
└── utils/          # Shared utilities
```

## Dependency Rule
`presentation/infrastructure → application → core`
Application services access repos via UoW properties (`uow.users`), never by importing concrete infrastructure.

## Key Files
- `registry/naming_registry.json` — Single source of truth for shared identifiers
- `opencode.json` — OpenCode configuration (plugins, MCP, permissions)
- `.opencode/oh-my-opencode.jsonc` — oh-my-opencode plugin configuration
- `Taskfile.yml` — Task runner commands (`task dev`, `task test`, `task lint`)
{%- endif %}
