# OpenCode Plugins

Place custom OpenCode plugins in this directory.

Each plugin should be a standalone module that extends OpenCode's capabilities.

See [OpenCode docs](https://opencode.ai/docs/plugins) for the plugin API.
