
# ADR-0004 — Pydantic Settings

## Status

Accepted

## Context

{{ '%Y-%m-%d' | strftime }}

We need a configuration system that supports environment variables, `.env` files, type validation,
and environment-specific overrides (local, test, prod).

## Decision

Use `pydantic-settings` for application configuration:

- Type-safe settings with validation
- Environment variable loading with prefix support
- `.env` file support via `python-dotenv`
- Nested settings objects for logical grouping
- Settings hierarchy: defaults → `.env` → environment variables → CLI flags

## Consequences

### Positive

- Fail-fast on misconfiguration at startup
- Full IDE autocompletion on settings
- Environment-specific settings classes (LocalSettings, ProdSettings)

### Negative

- Slight startup overhead for validation
