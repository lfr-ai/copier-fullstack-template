# GSD-2 Preferences

## Project Overview

- **Name**: {{ project_name }}
- **Type**: Fullstack Python + TypeScript application
- **Backend**: Python {{ python_version }}+ / FastAPI / SQLAlchemy 2.0+ / Clean Architecture
- **Frontend**: TypeScript / Vite / React 19 / Tailwind CSS v4
- **Package Managers**: UV (Python), Bun (frontend)
- **Container Runtime**: {{ container_runtime }}

## Architecture

Clean Architecture with strict Dependency Rule:
- `core/` → Pure domain (entities, enums, interfaces) — ZERO external deps
- `application/` → Use cases, services, DTOs — imports from core only
- `infrastructure/` → DB repos, cache, HTTP clients — implements core interfaces
- `presentation/` → API routes, CLI — FastAPI/Click allowed
- `config/` → Settings, DI container
- `utils/` → Shared helpers — stdlib + third-party only

Dependency direction: `infrastructure → presentation → application → core`

## Conventions

- Python: type hints everywhere, Google-style docstrings, keyword-only args (`*`),
  `from __future__ import annotations`, no `Any` type, structured logging with structlog
- TypeScript: strict mode, no `any`, Tailwind CSS v4
- Shell: `.zsh` extension, `emulate -L zsh`, `setopt ERR_EXIT PIPE_FAIL`
- Constants: plain `UPPER_SNAKE_CASE` (no `Final` annotation)
- Enums: `StrEnum` + `@unique` + `auto()`

## Verification Commands

```
task test              # Run ALL tests
task test:unit         # Backend unit tests
task test:integration  # Integration tests
task test:coverage     # Tests with coverage report
task lint              # All linters
task format            # Format all code
task typecheck         # Type checking
task registry:check    # Verify generated constants are up to date
```

## Model Preferences

- Default: Claude Sonnet 4
- Complex reasoning: Claude Opus 4
- Quick edits: Claude Haiku

## Behavior

- Always run `task lint` after editing Python files
- Always run `task registry:check` after changing naming_registry.json
- Never use `print()` — use structured logging
- Never hardcode field/column names — use registry constants
- Never use raw SQL — use SQLAlchemy ORM

## Agent Integration

GSD shares the same conventions as the GitHub Copilot agents in `.github/agents/`:
- **Skills**: `.github/skills/` (clean-architecture, python-conventions, testing-conventions, naming-registry)
- **Instructions**: `.github/instructions/` (architecture, coding, frontend, security, shell, testing, scaffolding)
- **Hooks**: `.github/hooks/` (tool-guardian, session-logger, secrets-scanner, auto-format, license-checker, context-saver)

Both GSD and Copilot agents follow the same Clean Architecture rules and coding conventions.
