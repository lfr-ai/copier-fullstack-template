# Local-only frontend overrides (never commit real frontend/.env.local)
# Copy if needed:
#   cp frontend/.env.local.example frontend/.env.local

# Example: point frontend to a different local backend
# VITE_API_BASE_URL=http://localhost:8000/api/v1

# Example: local feature toggles / diagnostics
# VITE_ENVIRONMENT=local
# VITE_APP_VERSION=0.0.0-local
