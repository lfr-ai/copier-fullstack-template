# Vite env vars for dev builds: vite build --mode dev
VITE_ENVIRONMENT=dev
{%- if api_style in ('rest', 'both') %}
VITE_API_BASE_URL=/api/v1
{%- endif %}
{%- if api_style in ('graphql', 'both') %}
VITE_GRAPHQL_URL=/graphql
{%- endif %}
VITE_APP_VERSION=0.0.0-dev
