{% if use_storybook %}import type { Preview } from '@storybook/react-vite';
import '../src/presentation/styles/tailwind.css';
import '../src/presentation/styles/main.css';

const preview: Preview = {
parameters: {
controls: {
matchers: {
color: /(background|color)$/i,
date: /Date$/i,
},
},
a11y: {
config: {
rules: [
{ id: 'color-contrast', enabled: true },
],
},
},
},
tags: ['autodocs'],
};

export default preview;
{% endif %}
