/**
 * Application stores barrel.
 *
 * Client-only state lives here. Server state uses TanStack Query instead.
 * Theme state is managed by ThemeProvider (presentation/components/theme-provider.tsx).
 */
