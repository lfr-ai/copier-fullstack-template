---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

# TypeScript Conventions

- Keep strict typing and avoid `any`.
- Prefer explicit domain types and generics.
- Use `unknown` at boundaries and narrow safely.
- Keep public contracts stable and documented.
- Prefer named exports for discoverability.
