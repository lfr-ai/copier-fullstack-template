---
paths:
  - "**/*.py"
---

# Python Conventions (Alias)

- Use explicit typing and avoid `Any` where possible.
- Use `from __future__ import annotations` in modules.
- Keep functions small and descriptive, with focused responsibilities.
- Use exception chaining for context-preserving error handling.