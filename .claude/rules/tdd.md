---
paths:
  - "template/backend/tests/**/*.py"
  - "scripts/**/*.py"
---

# TDD Rules

- Prefer writing failing tests before implementation changes.
- Add regression tests for every bug fix.
- Keep tests deterministic, isolated, and behavior-focused.